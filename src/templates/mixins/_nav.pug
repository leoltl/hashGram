include ./icons

mixin _nav(authUser)
  nav
    .logo #hashGram
    .nav__links
      a(href="/" id="home")
        +homeIcon()
      a(href="#" id="explore")
        +searchIcon()
      a(href="/new-post" id="new-post")
        +newPostIcon()
      a(href="#" id="activity")
        +activityIcon()
      if authUser
        a(href=`/${authUser.handle}` id='profile')
          +profileIcon()
      else 
        a(href="/signin" id="signin")
          +loginIcon()